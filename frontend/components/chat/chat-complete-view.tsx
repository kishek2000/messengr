/** @jsxImportSource @emotion/react */

import { mq } from '../../styles/mq';

export const ChatCompleteView = () => {
  return (
    <section
      css={mq({
        width: '100%',
        background: '#030610',
        height: '100%',
      })}
    ></section>
  );
};
